# Argon2 Types
ARGON2_D = get_argon2_d()
ARGON2_I = get_argon2_i()
ARGON2_ID = get_argon2_id()

# Argon2 Version
ARGON2_VERSION = get_argon2_version()

# Recommended Defaults (OWASP Recommendations)
ARGON2_DEFAULT_T_COST = get_argon2_default_t_cost()
ARGON2_DEFAULT_M_COST = get_argon2_default_m_cost()
ARGON2_DEFAULT_PARALLELISM = get_argon2_default_parallelism()
ARGON2_DEFAULT_HASH_LEN = get_argon2_default_hash_len()
ARGON2_DEFAULT_SALT_LEN = get_argon2_default_salt_len()